# SONIQ Bullet Bank

Raw, unpolished bullets. Keep them generalised — see `BOUNDARIES.md`.
Tag each bullet with themes in `[brackets]` so they're easy to grep when tailoring.

## Frontend Architecture

- Refactored React/TypeScript feature modules into layered hooks and pure UI components, reducing large component complexity and improving maintainability. `[react][typescript][refactoring][architecture]`

## Cloud Cost Optimisation

- Implemented scheduled shutdown/startup automation for non-production AWS environments using Lambda and EventBridge to reduce unnecessary infrastructure spend. `[aws][lambda][eventbridge][cost]`

## (add more themes as you go)

-
